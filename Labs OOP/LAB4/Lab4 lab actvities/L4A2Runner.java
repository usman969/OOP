package com.company;

public class L4A2Runner {
    public static void main (String[] args) {
        rangle rect = new rangle();
        rect.setLength(5);
        rect.setWidth(10);
        System.out.println("Area of rangle is " + rect.area());
        System.out.println("Width of rangle is " + rect.getWidth());
    }
}
